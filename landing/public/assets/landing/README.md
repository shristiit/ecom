Add landing page images here using these filenames:

- `hero-dashboard.png`
- `conversation-channels.png`
- `workflow-governance.png`
- `ops-overview.png`
- `integrations-strip.png`
- `final-cta-bg.png`

The site already handles missing files with premium placeholder panels, so the layout will not break before these assets are added.
